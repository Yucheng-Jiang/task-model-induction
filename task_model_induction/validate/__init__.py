"""Standalone deterministic validators for stage outputs.

Each module is self-contained (standard library only) so it can be copied into
the Codex sandbox and run as a plain script against a candidate JSON file.
"""
